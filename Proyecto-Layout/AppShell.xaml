<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Proyecto_Layout.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:Proyecto_Layout"
    Title="Proyecto_Layout">
    <!-- FlyoutBehavior="Disabled" -->
    
    <!-- Menú Lateral -->
    <FlyoutItem Title="HOME">
        <ShellContent ContentTemplate="{DataTemplate local:MainPage}" />
    </FlyoutItem>
    <FlyoutItem Title="ABSOLUTE LAYOUT">
        <ShellContent ContentTemplate="{DataTemplate local:Pageone}" />
    </FlyoutItem>
    <FlyoutItem Title="GRID LAYOUT">
        <ShellContent ContentTemplate="{DataTemplate local:Pagetwo}" />
    </FlyoutItem>
    <FlyoutItem Title="FLEX LAYOUT">
        <ShellContent ContentTemplate="{DataTemplate local:Pagethree}" />
    </FlyoutItem>
    <FlyoutItem Title="STACK LAYOUT">
        <ShellContent ContentTemplate="{DataTemplate local:Pagefour}" />
    </FlyoutItem>

</Shell>
